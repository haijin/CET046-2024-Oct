import pandas as pd
from sklearn.linear_model import LinearRegression


df = pd.read_csv('DBS_SingDollar(1).csv')
X = df.loc[:,['SGD']]
Y = df.loc[:,['DBS']]

model = LinearRegression()
model.fit(X,Y)
coef = model.coef_
intercept = model.intercept_